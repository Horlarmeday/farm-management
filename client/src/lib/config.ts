// Configuration for the application
export const config = {
  // Set to true to use mock data instead of API calls (useful when backend is not running)
  useMockData: true,

  // API base URL
  apiBaseUrl: 'http://localhost:5000',

  // App settings
  appName: 'Kuyash Farm Management System',
  version: '1.0.0',
};
